name: Deploy Site

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Set up Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '14'

      - name: Install dependencies
        run: npm install

      - name: Build and inject webhook URL
        run: |
          echo "WEBHOOK_URL=${{ secrets.DISCORD_WEBHOOK_URL }}" > .env
          npm init
          npm run build
